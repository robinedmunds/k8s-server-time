apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: server
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: roblobob/k8s-server-time-server:c99c319ffc634f602976e7839e9397046231cb37
          ports:
            - containerPort: 3000
